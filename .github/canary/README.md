# CI canary

`.flag` holds the documented `find` traversal action used by the local-parity health probe (see issue #1, "build cache check"). It is a single-token file required by `tools/verify-env.sh`.
